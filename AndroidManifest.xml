<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
      package="com.internal.transmit"
      android:versionCode="1"
      android:versionName="1.0">
      
    <uses-permission android:name="android.permission.RECEIVE_SMS" />
    <uses-permission android:name="android.permission.SEND_SMS" />
    
    <application android:label="@string/app_name" 
        android:icon="@drawable/icon">
        <activity android:name="Splash"
                  android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        
        <activity android:name="MainActivity" android:label="@string/app_name"></activity>
        <activity android:name="LoginActivity" android:label="@string/app_name"></activity>
        <activity android:name="SendMessageActivity" android:label="@string/app_name"></activity>
        <activity android:name="OutboxActivity" android:label="@string/app_name"></activity>
        <activity android:name="InboxActivity" android:label="@string/app_name"></activity>
        <activity android:name="SettingManagerActivity" android:label="@string/app_name"></activity>
        
        <receiver android:name="com.internal.transmit.PrivilegedSmsReceiver"
            android:permission="android.permission.BROADCAST_SMS">
            <intent-filter android:priority="1000">
                <action android:name="android.provider.Telephony.SMS_RECEIVED" />
            </intent-filter>
        </receiver>
    </application>
</manifest> 
